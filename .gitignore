# Build output and local demo/verify run directories
/build/
/dist/
/episodes/*
!/episodes/0001-after-git-push/
!/episodes/0001-after-git-push/**

# Go
*.test
*.out
coverage.txt

# Secrets — never commit real provider credentials. .env.example is the template.
.env
*.env
!.env.example
.env.mvp.local

# M3 Work Ledger - DaVinci Resolve MCP, OmniVoice, and Replay Hardening

> Running ledger for Milestone M3. Keep entries evidence-backed and bounded to
> disabled-by-default provider lanes, replay hardening, provider capability
> safety, and takeover reproducibility.

Mission: make Animus News ready for optional professional finishing and future
local/multilingual voice generation by adding safe provider boundaries for
DaVinci Resolve MCP and OmniVoice, while preserving all M1/M2 gates and keeping
live publishing impossible.

## Initial state reconstruction

Date: 2026-06-17

Current branch: `m1-openshorts-integration`

Baseline commit: `930228b`

Baseline verification:

| Command | Result | Notes |
| --- | --- | --- |
| `git status --porcelain` | pass | Clean working tree before M3 edits. |
| `git branch --show-current` | pass | `m1-openshorts-integration`. |
| `git log --oneline -n 16` | pass | M2 commits `d7e57a1..930228b` visible on top of M1. |
| `make verify` | pass | `M2 VERIFY: GREEN`; mock success reached `published_dry_run_complete`; injected storyboard failure blocked at `storyboard_image`; secret scan found no findings. |
| `make verify-m2-local` | pass | Local adapter contract checks and workflow determinism checks passed. |
| `go vet ./...` | pass | No findings. |
| `go test ./...` | pass | All packages green. |

Current M1/M2 packages:

- `internal/shortform`: typed short-form artifact contracts, schema validation,
  canonical content hashing, approval transforms, and artifact validation.
- `internal/shortform/providers`: provider interfaces and deterministic mocks.
- `internal/shortform/providers/localexec`: path containment, output hashing, and
  redaction helpers for local execution boundaries.
- `internal/shortform/providers/render`: FFmpeg local render adapter, disabled by
  default.
- `internal/shortform/providers/subtitles`: faster-whisper sidecar boundary,
  disabled by default.
- `internal/shortform/providers/uploadpost`: Upload-Post dry-run adapter; live
  mode refused.
- `internal/shortform/gates`: pure content, release, and invariant gates.
- `internal/shortform/activities`: Temporal-safe activity boundaries.
- `internal/workflows/shortform.go`: deterministic short-form workflow.
- `cmd/animus-news`: CLI for validation, demo, secret scan, and workflow
  operations.

Intended M3 scope:

- Improve replay evidence beyond the M2 deterministic result fixture where
  feasible without requiring a default Temporal dev server.
- Add a disabled-by-default DaVinci Resolve MCP render/finishing provider
  boundary with allowlisted conceptual MCP tools and dry-run tests.
- Add a disabled-by-default OmniVoice local/sidecar voice provider boundary with
  consent-sensitive request validation and dry-run/fake sidecar tests.
- Add provider capability metadata and fail-closed provider selection safety.
- Add M3 verification targets, status docs, ADRs, and takeover instructions.

Explicit non-goals:

- No real Seedance execution.
- No real ElevenLabs execution.
- No real Upload-Post scheduled/public publish.
- No social platform upload.
- No browser automation.
- No DaVinci Resolve GUI dependency in default tests.
- No OmniVoice model download in default tests.
- No arbitrary MCP tool execution.
- No arbitrary Python/Lua/script execution.
- No provider output may bypass artifact validation, gates, production QA, or
  human release approval.

Risks:

- Full `worker.WorkflowReplayer` JSON-history replay may still require a
  Temporal dev-server fixture capture; if so, M3 must document the exact M4
  requirement and improve offline evidence instead.
- DaVinci Resolve MCP is not present in this environment, so M3 must implement a
  strict client abstraction and dry-run/stub tests rather than real GUI
  automation.
- OmniVoice model assets are not present and must not be downloaded during
  verification; sidecar tests must use fake local fixtures only.
- Capability metadata must not become an authorization bypass; gates and
  artifact validation remain authoritative.

## Phase 1 - Replay hardening

Implemented:

- Extended `TestShortFormWorkflowDeterministicResultFixture` to include workflow
  notes/state-transition evidence in the canonical fixture hash.
- Added `TestShortFormWorkflowBlockedPathFixtures` for deterministic fixture
  hashes across storyboard rejection, release denial, and render-gate block
  paths.
- Added `TestShortFormWorkflowDoesNotCallExternalProviderBoundaries` to ensure
  the short-form workflow source does not directly reference DaVinci, OmniVoice,
  MCP, or process execution.

Validation:

- `go test ./internal/workflows -run 'TestShortFormWorkflow(ReplayIsDeterministic|DeterministicResultFixture|BlockedPathFixtures)|TestShortFormWorkflowDoesNotCallExternalProviderBoundaries' -count=1` -> pass.

Remaining partial:

- Full `worker.WorkflowReplayer` JSON-history replay still requires a safe
  Temporal dev-server history fixture capture. M4 should add a gated fixture
  recorder and committed JSON history replay test.

## Phase 2 - DaVinci Resolve MCP provider boundary

Implemented:

- `internal/shortform/providers/mcp` with an allowlist for Resolve MCP tools and
  a dry-run MCP client.
- `internal/shortform/providers/render/davinci` implementing
  `providers.RenderProvider`.
- Disabled-by-default `DaVinciResolveConfig`.
- `disabled|dry_run|local_mcp` mode shape.
- MCP URL validation, controlled input/output/project roots, path containment,
  and explicit start-render guard.
- Draft `short_render_manifest` output with DaVinci provider metadata and MCP
  tool plan.

Validation:

- `go test ./internal/shortform/providers/mcp ./internal/shortform/providers/render/davinci` -> pass.
- Tests cover disabled default, missing MCP URL, non-allowlisted tools, path
  traversal, dry-run manifest construction, start-render refusal, draft outputs,
  and production-QA gate preservation.

## Phase 3 - OmniVoice provider boundary

Implemented:

- `internal/shortform/providers/voice/omnivoice` implementing
  `providers.VoiceProvider`.
- Disabled-by-default `OmniVoiceConfig`.
- `disabled|dry_run|local_sidecar` mode shape.
- Required local binary and preinstalled local model directory.
- Consent checks for reference voice or voice-prompt workflows.
- Draft `voiceover_manifest` output with provider metadata, language, optional
  voice prompt/consent references, output path/hash, duration, format, and sample
  rate.

Validation:

- `go test ./internal/shortform/providers/voice/omnivoice` -> pass.
- Tests cover disabled default, missing binary/model, dry-run manifest
  validation, invalid sidecar output path blocking, consent metadata, reference
  audio allowed/hash requirements, draft output, and redaction.

## Phase 4 - Provider capability registry

Implemented:

- `internal/shortform/providers/capabilities` default registry.
- `animus-news provider-capabilities` CLI command.
- `make provider-capabilities` target.
- `make verify-m3` target for M3 provider boundary, registry, and replay checks.

Validation:

- `go test ./internal/shortform/providers/capabilities ./cmd/animus-news` -> pass.
- `make verify-m3` -> pass.

## Phase 5 - Verification and documentation

Implemented:

- Optional M3 metadata fields for `voiceover_manifest` and
  `short_render_manifest`.
- ADR-0009, ADR-0010, ADR-0011.
- `docs/reports/M3_status.md`.
- `CLAUDE.md`.

Validation:

- `go test ./internal/shortform/...` -> pass.
- `go test ./internal/workflows` -> pass.
- `go test ./cmd/animus-news` -> pass.
- `go test ./...` -> pass.
- `go vet ./...` -> pass.
- `make verify-m3` -> pass.
- `make verify` -> pass (`M3 VERIFY: GREEN`).
- `make verify-m2-local` -> pass after M3 edits.

Takeover note:

- Final handoff is verified from committed state with:
  `git status --porcelain`, `make verify`, `make verify-m2-local`,
  `go vet ./...`, `go test ./...`, `make verify-m3`, `rm -rf build dist`, and
  a final `git status --porcelain`.

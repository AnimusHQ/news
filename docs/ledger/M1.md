# M1 Work Ledger — OpenShorts → AnimusHQ/news Integration

> Running ledger for Milestone M1. Append-only by intent. Every non-trivial
> decision links to an ADR under `docs/adr/`. State must be reconstructable by
> another engineer from this file + the ADRs alone (takeover test).

Mission (bounded): integrate the **execution capabilities** referenced from
OpenShorts (subtitles, FFmpeg render/composite, 9:16 normalization, Upload-Post
publishing pattern) into the News control plane as **typed, validated, gated
contracts** that run **end-to-end on mock providers**, with every quality/release
gate enforced in code, every artifact typed/validated/hashed/versioned, and the
whole thing reproducible by a single verify command. M1 proves architecture,
contracts, and gates — **not** real provider execution (that is M2/M3).

---

## Phase 0 — Audit & reconcile

### Confirmed stack (verified from the repo, not assumed)

- **Backend:** Go (`go.mod` `module github.com/AnimusHQ/news`, `go 1.24.0`; toolchain present `go1.25.3`).
- **Orchestration:** Temporal Go SDK `go.temporal.io/sdk v1.42.0` (already a dependency; worker + workflow already exist).
- **Serialization/validation:** `gopkg.in/yaml.v3`; hand-written deterministic validators (no JSON-Schema library is currently vendored). `github.com/stretchr/testify` available for tests.
- **Build status (Gate P0):** `go build ./...` → **exit 0** (pass). `go test ./...` → **all green** (28 packages; 3 have `[no test files]`: `internal/activities`, `internal/models`, `internal/models/adapters`, `internal/worker`).
- **CLI binary:** `cmd/animus-news` (NOT `cmd/news`). Subcommands: `validate`, `validate-episode`, `extract-claims`, `dry-run`, `scan-secrets`, `worker`, `start-workflow`, `signal-human-qa`, `signal-release`, `query-state`.
- **Object storage / immutability:** `internal/storage` already implements a content-addressed `LocalStore` with `ErrImmutableConflict` (same name + different content hash is rejected). Interface-compatible with future Postgres/S3.

### What already exists (relevant to M1)

| Concern | Location | Notes |
| --- | --- | --- |
| Common envelope | `internal/artifacts/types.go` (`Metadata`), `internal/artifacts/validate.go` (`ArtifactEnvelope`) | Fields: schema_version, episode_id, artifact_id, created_at, created_by, source_artifacts, content_hash, status. |
| Artifact status enum | `internal/artifacts/types.go` | `draft|approved|rejected|superseded` only. |
| Canonical episode validators | `internal/artifacts/validate.go`, `schema_validators.go` | Hand-written per-artifact validators for the 14 canonical files. |
| Existing canonical artifacts | `internal/artifacts/validate.go` `RequiredEpisodeFiles` | topic, research_pack, claims, editorial_brief, script, verification_report, multimodel_approval_report, human_qa_report, storyboard, asset_manifest, render_manifest, production_qa_report, publish_manifest, analytics_report. |
| Content hashing | `internal/storage/store.go` `contentHash`, `internal/render/preview.go`, `internal/artifacts/dependencies.go` `FileContentHash` | All are **raw-bytes** sha256 (`sha256:` prefix). No canonical-JSON hash that excludes the `content_hash` field exists yet. |
| State machine | `internal/workflows/state_machine.go` | Canonical episode lifecycle with `humanRequired` transition rules; long-form oriented (backlog→…→published). |
| Temporal workflow | `internal/workflows/episode.go` | `EpisodeLifecycleWorkflow`: validate → mock council → human-QA signal → production QA → release signal → dry-run publish. Mock activities. |
| Activities | `internal/activities/episode.go` | Mock/deterministic activities + domain wrappers. |
| Worker | `internal/worker/worker.go` | Registers workflow + 4 activities on task queue `animus-news-episode`. |
| Domain generators (style exemplars) | `internal/storyboard`, `internal/render`, `internal/productionqa`, `internal/publishing` | Pattern: typed `Input` → typed output (`File`/`Result`/`Report`/`ReleasePack`), deterministic, no external calls. |
| Audit | `internal/audit/audit.go` | Structured `Event`, categories, `MemorySink`, redaction; release-approval events require a `human:` actor. |
| Multimodel ≥2 verifiers | `internal/artifacts/schema_validators.go` `validateMultimodelApprovalArtifact` | Already enforces `len(model_panel) >= 2`. |
| Publish safety | `internal/artifacts/validate.go`, `schema_validators.go` | Public visibility requires `human_release_approval`; scheduled requires `scheduled_at` + approval. |
| Synthetic disclosure | `internal/publishing/pack.go` | Uses `synthetic_disclosure_required` / `synthetic_disclosure` (naming differs from M1's `ai_disclosure_required`). |

### Integration source spec

`/home/guest/projects/openshorts/animus_news.md` ("OpenShorts Production Pipeline
Specification") is the integration context document. Relevant sections read and
used to shape contracts: §8 Artifact Types, §9 Status Model (`generated, draft,
in_review, needs_revision, rejected, approved, locked, production_candidate,
published`), §12 Data Contracts (campaign, script_package, storyboard_package,
storyboard_review, shot_manifest, final_qa, production_approval), §15 Provider
Interfaces (VisualProvider, VoiceProvider, SubtitleProvider, RenderProvider).
OpenShorts is **execution reference only** — its FastAPI backend, job queue,
Gemini-first logic, generate→publish shortcut, and client-side key storage are
NOT imported (per §2 role distinctions).

### Delta list (this document ↔ repo reality)

D-01. **CLI path.** Driver doc says `go run ./cmd/news demo …`; the real binary is
`cmd/animus-news`. **Resolution:** add the `demo` subcommand to `cmd/animus-news`;
document the exact command in the report. No `cmd/news` created (avoid surface).

D-02. **Artifact status model.** Repo has 4 statuses; M1 envelope requires
`draft|in_review|approved|rejected|superseded|locked` (adds `in_review`, `locked`).
**Resolution:** extend `ArtifactStatus` + `validArtifactStatus` with the two new
states (backward-compatible; existing tests only assert draft/approved + reject
unknown). See ADR-0002.

D-03. **Canonical content hashing.** No deterministic canonical-JSON hash that
excludes the `content_hash` field exists. M1 §7 requires one. **Resolution:** new
`internal/shortform/contenthash` computing sha256 over canonicalized (sorted-key)
JSON with the hash field removed. See ADR-0002.

D-04. **JSON Schema.** Repo validates by hand-written Go; no JSON-Schema library is
vendored, and M1 requires "Go types + JSON Schema + validators" while also
requiring offline reproducibility (no network egress to pass tests). **Resolution:**
ship real JSON Schema documents as committed contracts AND a small embedded
schema interpreter (no new dependency) that validates instances against them, so
the schema is load-bearing for the accept/reject tests. See ADR-0003.

D-05. **New short-form artifacts are additive, not part of the required bundle.**
The existing `episode-0001` bundle does not contain the 8 new artifacts; adding
them to `RequiredEpisodeFiles` would break existing validation. **Resolution:**
implement them in a dedicated `internal/shortform` package and a separate run
directory; register single-file recognition only where safe. See ADR-0001.

D-06. **Disclosure field naming.** Existing publishing uses
`synthetic_disclosure_required`; M1 §4.8/§7 uses `ai_disclosure_required`.
**Resolution:** new artifacts use `ai_disclosure_required` (matches M1 + OpenShorts
spec). Legacy field left untouched. Recorded as terminology delta.

D-07. **Workflow shape.** The existing `EpisodeLifecycleWorkflow` is long-form and
does not cover the §9 short-form activity list. **Resolution:** add a dedicated
`ShortFormWorkflow` + activities; leave the existing workflow intact. See ADR-0004.

D-08. **Demo vs. live Temporal.** A live Temporal server is not available offline,
but the e2e demo (§Phase 5) must be runnable by a single command. **Resolution:**
the demo runs an in-process pipeline runner that shares the exact activity/gate
functions with the Temporal workflow; the durable workflow itself is proven by the
Temporal `testsuite` env + a replay test (no server needed). See ADR-0004.

### Open questions / assumptions (smallest reasonable assumption marked)

- **A-01 (assumption):** `episode-0001` is the demo episode id used by §Phase 5
  (`--episode episode-0001`). The repo's existing bundle is `episodes/0001-after-git-push`.
  Assumption: the demo synthesizes short-form inputs from a small fixture under the
  episode and writes outputs to a run dir; it does not require the legacy bundle to
  already contain short-form artifacts. Marked; revisit if a real short-form episode
  bundle is added.
- **A-02 (assumption):** "≥2 distinct verifier identities" for final sign-off is
  satisfied by the existing multimodel report rule (`model_panel >= 2`) plus a
  release gate requiring a distinct `human:` approver. We additionally enforce a
  generic `MultiVerifierGate` for the short-form release path.
- **A-03 (assumption):** Mock outputs use fake-but-valid hashes/paths (no real
  bytes). The render target is fixed at 1080×1920 / 9:16 / 30fps / h264 per §7.
- **A-04 (decision, not blocking):** No new Go module dependency is added in M1
  (ADR-0003), to guarantee the takeover test reproduces offline.

### Gate P0 — status: **GREEN**

- `go build ./...` result recorded (pass, exit 0).
- Delta report committed (this section).
- No code changes beyond docs scaffolding in Phase 0.

---

## Phase 1 — Artifact contracts (types + schema + validation + hashing)

Implemented in `internal/shortform`:

- **Typed structs** (`artifacts.go`): all 8 short-form artifacts embed the common
  `Envelope` and implement `Artifact` (`Kind()`, `EnvelopeRef()`). Shared value
  objects: `ProviderRef`, `RenderTarget`, `MediaOutput`, `RendererRef`,
  `RenderOutput`, `RiskAcceptance`, `SubtitleChecks`, `CandidateComponent`.
- **Content hashing** (`contenthash/`): `Compute`/`Verify`/`Canonicalize` — sha256
  over canonical (sorted-key) JSON with the `content_hash` field excluded. `Stamp`
  writes the hash onto an artifact idempotently.
- **JSON Schema** (`schemas/*.schema.json`, embedded via `go:embed`) + a
  dependency-free Draft-2020-12-subset interpreter (`schema/`) that fails closed on
  unsupported keywords.
- **Validation** (`validate.go`): `Validate(Artifact)` = schema + envelope semantics
  (RFC3339 created_at, created_by form, sha256 prefix) + light kind semantics
  (unique scene_ids; aspect match; production_candidate locked+immutable).
  `ValidateFile` adds full content-hash integrity from bytes. `ValidateBytes`,
  `KindFromFilename`, `SchemaFor`, `KnownKinds` exposed for CLI/demo.
- **Status model** (delta D-02): added `in_review`, `locked` to
  `internal/artifacts/types.go` + `validArtifactStatus`; added
  `ArtifactStatus.IsTerminalImmutable()`.

CLI: `animus-news validate-shortform <file>` validates one short-form artifact.

### Gate P1 — status: **GREEN**

Tests (run `go test ./internal/shortform/...`):
- `contenthash`: `TestComputeIsStableAcrossRuns`, `TestComputeExcludesContentHashField`,
  `TestComputeIsKeyOrderIndependent`, `TestComputeRoundTripThroughHashField`,
  `TestVerifyDetectsTampering`, `TestCanonicalizeRemovesHashAndSortsKeys`.
- `schema`: 9 keyword-level tests incl. `TestCompileRejectsUnknownKeyword`.
- `shortform`: `TestAllSchemasCompile` (8 schemas), `TestArtifactsValidateStampAndRoundTrip`
  (round-trip marshal→unmarshal→DeepEqual + ValidateFile per artifact),
  `TestArtifactHashIsDeterministicAndExcludesHashField` (stable hash per artifact),
  `TestArtifactsRejectInvalidAgainstSchema` (one reject case per artifact).
- `go build ./...` and `go test ./...` green; existing artifact tests unaffected by
  the status additions.

## Phase 2 — Provider interfaces + mocks

Implemented in `internal/shortform/providers`:

- **6 interfaces** (`providers.go`): `StoryboardImageProvider`, `VisualVideoProvider`,
  `VoiceProvider`, `SubtitleProvider`, `RenderProvider`, `PublishingProvider`, each
  with a typed request struct and a deterministic timestamp input (`Now`).
- **6 mocks** (`mocks.go`): deterministic, no network, fake-but-valid sha256
  hashes/paths, all emit **draft** artifacts authored by `system`/`model:*` (never a
  human approver — no self-approval). Compile-time `var _ Interface = Mock{}` checks.
- **Failure injection** (`Defect`): `DefectError` (all providers),
  `DefectSubtitleSyncFailed` (sync check false), `DefectRenderNoAudio` (no audio
  track), `DefectMissingDisclosure` (publish manifest omits disclosure) — the domain
  defects stay schema-valid so they exercise gate negatives, not schema negatives.

### Gate P2 — status: **GREEN**

Tests (`go test ./internal/shortform/providers/...`):
- `TestMocksEmitSchemaValidDraftArtifacts` — drives the full chain
  (storyboard→visual→voice→subtitle→render→publish) and asserts each emitted
  artifact passes `shortform.Validate`, is stamped, is `draft`, and is not human-authored.
- `TestMocksAreDeterministic`, `TestVisualShotsReferenceStoryboardImageHashes`,
  `TestDefectErrorIsInjectable`, `TestDomainDefectsStaySchemaValid`.

## Phase 3 — Gates as code

Implemented in `internal/shortform/gates` as pure `func(input) Result` (Result =
decision + machine-readable `Reason{code,message,field}`). No I/O.

**Content/release gates (§8)** — `content_gates.go`:
- `StoryboardImageGate` — blocks: manifest_missing, source_not_chatgpt_manual,
  operator_approval_missing, scene_image_missing, image_path_missing,
  image_hash_missing, image_not_approved, visual_review_failed.
- `VisualShotGate` — blocks: manifest_missing, provider_metadata_missing,
  prompt_missing, negative_prompt_missing, output_missing, duration_out_of_tolerance,
  reference_image_not_approved, scene_mapping_invalid, operator_approval_missing.
- `SubtitleGate` — blocks: manifest_missing, transcript_missing, captions_missing,
  word_timestamps_missing, safe_zone_failed, sync_failed, operator_approval_missing.
- `RenderGate` — blocks: manifest_missing, outputs_missing, output_hash_missing,
  resolution_invalid, aspect_invalid, fps_invalid, audio_track_missing,
  subtitles_not_readable, production_qa_not_approved.
- `ReleaseGate` — blocks: publish_manifest_missing, production_qa_not_approved,
  human_release_approval_missing, platforms_not_explicit, visibility_not_set,
  scheduled_at_missing, disclosure_text_missing (via AIDisclosureGate), dry_run_failed.

**Invariant gates (§4)** — `invariant_gates.go`:
- `SelfApprovalGate` (§4.6) — approver_missing, self_approval, non_human_approval.
- `ImmutabilityGate` (§4.9) — immutable_mutation (approved + locked).
- `AIDisclosureGate` (§4.8) — disclosure_text_missing, disclosure_not_present.
- `MultiVerifierGate` (§4.7) — insufficient_verifiers (default ≥2 distinct).
- `ProvenanceGate` (§4.1, §4.2) — research_pack_missing, claim_without_source.

### Gate P3 — status: **GREEN**

- Positive tests: `Test<Gate>Passes` for all 10 gates.
- Negative tests: `Test<Gate>Blocks` — each blocking condition above is exercised by
  a dedicated failing-input subtest (table-driven), asserting the specific reason
  code. Run `go test ./internal/shortform/gates/`.
- Coverage: **98.5%** of statements in the gates package
  (`go test -cover ./internal/shortform/gates/`).

## Phase 4 — Temporal workflow + activities

_(populated during Phase 4)_

## Phase 5 — End-to-end mock episode demo

_(populated during Phase 5)_

## Phase 6 — Reporting & takeover

_(populated during Phase 6)_
